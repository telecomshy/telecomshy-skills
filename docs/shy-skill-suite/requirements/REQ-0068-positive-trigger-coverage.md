---
id: REQ-0068
title: 正例漏触发修正（#6 搭骨架 / #7 触发描述 / #8 是否该拆分）
skill: shy-skill-suite
status: done
kind: fix
iteration: 1
created: 2026-09-19
updated: 2026-09-19
blocked_by: []
related: [REQ-0067, REQ-0047, REQ-0035]
---

# REQ-0068 正例漏触发修正

## 问题与目标

`REQ-0067` 修好评测执行层后，全量 20 条的第一份可信结果显示：**负例 20/20 全 0.0（precision 1.0），问题只在正例漏触发 3 条**（且跨批次抖动）：

| 用例 | 触发率（近两轮全量） | 现状 |
| --- | --- | --- |
| `#6` 新建一个技能，先给我搭个骨架 | 0.0 / 0.33 | description 只说"新建或修改一个技能"，没覆盖"搭骨架 / 脚手架"说法 |
| `#7` SKILL.md 的 description 怎么写才能触发准？ | 0.33 / 0.0 | 只说"写或改技能的 SKILL.md"，没覆盖"调触发描述 / 触发精度" |
| `#8` 帮我看下这个技能够不够规范，要不要拆成多个 | 0.33 / 0.67 | 只说"校验技能结构"，没覆盖"是否该拆分"这类结构决策 |

目标：按**类别**（不抄测试句）补三个分支到 `description`，全量复测达到——三条触发率 ≥0.5、**负例仍全 0.0**、test 分不降。

## 触发与分支

- `SKILL.md` 的 `description`（唯一触发指针）。
- 用户说"新建技能搭个骨架 / description 怎么调 / 这个技能要不要拆"。

## 行为与步骤

1. 候选 E：在原 description 上补三处类别词——`新建或修改一个技能（含搭骨架 / 脚手架）`、`调触发描述（description）与触发精度`、`校验技能结构（含是否该拆分）`。
2. 临时部署候选（备份 → 替换 → 跑全量 20 条 × 3 次、4 并发、`skill-line`、钉模型、独立空 cwd、120s → 还原）。
3. 判定：`#6/#7/#8` ≥0.5、负例全 0.0、test 分不降 → 写入 `SKILL.md`；否则回退并记录。

## 脚本与资源

- 改 `skills/shy-skill-suite/SKILL.md`（仅 `description` 一行）。
- 评测：`optimize_description.py --runner cmd --cmd 'opencode run --model deepseek/deepseek-flash "{prompt}"' --detect-mode skill-line --trials 3 --workers 4 --timeout 120 --isolate-cwd`。

## 降级与边界

- 只加**类别**表述，不抄评测集里的具体句子（防过拟合）。
- `description` 硬限 1024 字符。
- 若加词唤醒负例（precision 降）→ 回退，宁可少一条正例。

## 验收标准

- [x] 全量复测：`#6` / `#7` / `#8` 触发率 ≥0.5 — （行为）候选轮：1.0 / 1.0 / 0.667；确认轮：1.0 / 0.667 / 0.667
- [x] 全量复测：负例仍全 0.0（`#107` 确认轮 0.333 < 0.5 仍判正确），test 分不降 — （行为）两轮 test 均 1.0
- [x] `SKILL.md` 的 `description` 已更新（210 字符）且 `validate_skill` ok — `check:skill-validate-ok`

## 范围外

- 不扩评测集、不改评分口径（`REQ-0044`）。
- 不改 `description` 之外的内容。

## 迭代记录

| 轮次 | 日期 | 本轮改动 | 证据 | 结论 / 下一步 |
| --- | --- | --- | --- | --- |
| 1 | 2026-09-19 | 候选 E 临时部署 + 全量对照（20 条 × 3 次、4 并发、skill-line、钉模型、隔离） | **score 1.0 / P 1.0 / R 1.0 / F1 1.0（train 与 test）**；`#6`=1.0、`#7`=1.0、`#8`=0.667；负例全 0.0 | 采用 |
| 2 | 2026-09-19 | 写入 `SKILL.md` 并**确认复测** | 复测同样 **20/20、0 failures、双 split 1.0**；`#6`=1.0、`#7`=0.667、`#8`=0.667；负例仅 `#107`=0.333（<0.5 仍判正确）；Gate 36/36、selftest 26/26 | **收敛** |

## 备注 / 待办

- 基线（`REQ-0067` 复测）：test 1.0 / train 0.75；负例 20/20 全 0.0。
- 泛化要求：三处补词分别代表「脚手架场景」「触发描述调优」「拆分决策」三类说法。
