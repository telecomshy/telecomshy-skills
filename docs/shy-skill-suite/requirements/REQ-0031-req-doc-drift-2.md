---
id: REQ-0031
title: 复审发现的 REQ 文档漂移修正（第二批）
skill: shy-skill-suite
status: out-of-scope
iteration: 2
created: 2026-09-17
updated: 2026-09-17
blocked_by: []
related: [REQ-0006, REQ-0009, REQ-0012, REQ-0014, REQ-0015, REQ-0018, REQ-0020, REQ-0023, REQ-0026, REQ-0027, REQ-0028, REQ-0030]
---

# REQ-0031 复审发现的 REQ 文档漂移修正（第二批）

## 问题与目标

2026-09-17 第二轮全量复审（30 份 REQ 逐条扫）在**需求轴**发现的 12 处 REQ 文档与当前实现不符。全部已证伪确认。

**根因是系统性的**：REQ-0026~0030 那轮大改（资源清单精简、管线串去重、「不落盘」改词、命令 5→4、退出码改幂等）动了**共有的文本**，而老 REQ 的验收标准是按当时的文本写的 → 一批 `[x]` 静默变假。这正是 Spec 轴改「无条件全量」要抓的东西。

目标：让每份 REQ 的验收与今天的实现一致，`done` 不再说谎。

## 触发与分支

- 复审（`reviewing-skills.md`）Step 3 收工后，用户确认落盘。
- 用户说"把这些 REQ 文档再对齐一次"。

## 行为与步骤

只改 `docs/shy-skill-suite/requirements/` 下的文档，**不改技能文件**。

1. **`REQ-0006:59`**：「`SKILL.md` 资源列出 `grilling.md`」→「路由表含逼问入口（指向 `grilling.md`）」——REQ-0028 把资源清单精简为"只列路由表未覆盖的"。
2. **`REQ-0009:56`**：同上，「`writing-skills.md`」→「路由表含写作入口（指向 `writing-skills.md`）」。
3. **`REQ-0012:39`**：备注登记「退出码语义已被 `REQ-0027` 部分修订（`agent_runner` 运行错误 rc=1；`scaffold` 已存在 rc=0/`skipped`）」。
4. **`REQ-0014:50,52`**：验收 #1 删去「实测 95 字符」（或改 ≤500）；#3 由字面「需要 Python 3（仅标准库）」改为「写明最低 Python 版本（≥3.7）与手工降级路径」。
5. **`REQ-0015:56`**：验收 #3「4 条原则名 + 各自指针」→「4 条原则名 + **被去重的两条**各带指针」（实际 `SKILL.md:28-29` 有指针，`:30-31` 是自包含定义）。
6. **`REQ-0018:79`**：判据短语 `grep "只呈现、不落盘"` → `grep "只呈现、不回写 REQ"`（`REQ-0028` 改的词）。
7. **`REQ-0020:40`**：行为步骤「五个命令各进入哪个分支」→「四个命令」。
8. **`REQ-0023:50`**：加订正标注——`REQ-0028` 把管线串去重后，`SKILL.md` 含「呈现」的处数由 3 变 1（仅 `:13`）。
9. **`REQ-0026:64`**：criterion #10 的标注词未统一（精确串只命中 REQ-0020，其余用「复审补记」/「REQ-0027 修订」/无日期）→ 要么统一标注词，要么把判据写成「等价标注即可」；给 `REQ-0014` 补日期。
10. **`REQ-0028:82`**：迭代记录证据「SKILL.md 49→45 行」不实（git 基线 48 行）→ 改为「48→45」或删绝对数字。
11. **`REQ-0030:88`**：criterion #10「25 份 REQ」→「全部 REQ（当前 30 份）」。
12. **`REQ-0027`「脚本与资源」**：补 `validate_skill.py`（该 REQ 的实现把它的错误流也改成了 stderr，属同类一并修，但清单漏列）。

**统一要求**：就地加「2026-09-17 复审订正」标注，`updated` 改当天；**不**追加迭代记录（与 `REQ-0023` 改 `REQ-0012` 的既有做法一致）。

## 脚本与资源

- 改 `docs/shy-skill-suite/requirements/REQ-0006/0009/0012/0014/0015/0018/0020/0023/0026/0027/0028/0030-*.md`。
- 不新增文件、不改技能文件。

## 降级与边界

- **只做文档对齐**，不改行为语义、不改技能文件。
- 标注里**引用旧值**（如「原写 5 个」）是刻意的，判据针对的是"把旧值当**当前**计数"，不是消灭旧值字面。
- 第 9 条的标注词统一方式二选一，不两套并存。

## 验收标准

- [ ] `REQ-0006`/`REQ-0009` 的该条验收改为「路由表含…入口」，且 `SKILL.md` 路由表确有对应行。
- [ ] `REQ-0012` 备注含 `REQ-0027` 对退出码的修订说明。
- [ ] `REQ-0014` 验收 #1 不含具体字符数（或为 ≤500）；#3 与 `SKILL.md` 的 `Python ≥ 3.7` 一致。
- [ ] `REQ-0015` 验收 #3 与 `SKILL.md:28-31` 的实际情况一致（2/4 带指针）。
- [ ] `grep "只呈现、不回写 REQ"` 命中 `lifecycle.md`，且 `REQ-0018` 的判据用当前用语。
- [ ] `REQ-0020` 内不再把「五个命令」当作**当前**计数。
- [ ] `REQ-0023` 的「呈现」处数标注与 `grep -c 呈现 SKILL.md`（=1）一致。
- [ ] `REQ-0028` 的证据行与 `git cat-file -p HEAD:skills/shy-skill-suite/SKILL.md | Measure-Object -Line`（=48）一致。
- [ ] `REQ-0030` 的计数为「全部 REQ（当前 30 份）」，与 `Get-ChildItem docs/shy-skill-suite/requirements`（=30）一致。
- [ ] `REQ-0027` 的「脚本与资源」含 `validate_skill.py`。
- [ ] 被修 REQ 的 `updated` 为 `2026-09-17`。
- [ ] `python skills/shy-skill-suite/scripts/track_requirements.py --root .` → `status: ok`、退出码 0。

## 范围外

- 不改技能文件（`SKILL.md` / `references/` / `scripts/` / `assets/`）。
- 不改 `status` 语义、不重开已 `done` 的 REQ。
- 不追加迭代记录（只就地标注）。

## 迭代记录

| 轮次 | 日期 | 本轮改动 | 证据 | 结论 / 下一步 |
| --- | --- | --- | --- | --- |
| 1 | 2026-09-17 | 落需求（2026-09-17 第二轮复审需求轴 12 条 findings） | — | 待开工 |
| 2 | 2026-09-17 | **退休（out-of-scope）**：全部验收标准均为**文档措辞 / 计数一致性**，不改变行为契约 → 按 `reviewing-skills.md` Step 3 第 3 条「自洽项不复扫、攒批、不新开 REQ」不再执行。且其引用的计数已过期（如判据写「当前 30 份 REQ」，仓库现为 39 份）——继续追就是漂移循环本身。 | 前置：`REQ-0026` 已收敛（同类第一批） | **out-of-scope（退休）** |

## 备注 / 待办

- 来源：2026-09-17 第二轮复审 findings（需求轴 P1 ×1 已当场修，P2 ×12 落于此）。报告：`skills/shy-skill-suite-workspace/iteration-2/report.html`。
- 与 `REQ-0026` 同类（都是"REQ 文档对齐"），区别：REQ-0026 修的是**首轮**发现的假 done，本 REQ 修的是**大改引入的新漂移**。
- 本 REQ 的存在本身是 Spec 轴全量扫有效的证据：12 条里 10 条是 REQ-0026~0030 那轮改动**顺带**弄假的。
- **顺序依赖**：第 9/10/11 条分别修 `REQ-0026` 的 criterion #10、`REQ-0028` 的迭代记录证据、`REQ-0030` 的 criterion #10 —— 故这三份 REQ **要等本 REQ 落地后才能收口为 `done`**（它们现在停在 `in-progress`）。
- **处置（2026-09-17）**：**退休（out-of-scope）**：全部验收标准均为**文档措辞 / 计数一致性**，不改变行为契约 → 按 `reviewing-skills.md` Step 3 第 3 条「自洽项不复扫、攒批、不新开 REQ」不再执行。且其引用的计数已过期（如判据写「当前 30 份 REQ」，仓库现为 39 份）——继续追就是漂移循环本身。
