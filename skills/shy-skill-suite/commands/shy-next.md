---
description: 技能开发套件 · 列出需求 frontier（track_requirements）
---

调用 `skill` 工具加载 `shy-skill-suite` 技能，然后运行 `python "<SKILL_DIR>/scripts/track_requirements.py" --root .`，列出当前能开始的 REQ（frontier）、被阻塞的、以及悬空 `blocked_by`。

技能（可选）：$ARGUMENTS
