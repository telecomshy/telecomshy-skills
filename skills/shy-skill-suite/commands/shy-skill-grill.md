---
description: 技能开发套件 · 逼问一个技能该做什么（进入 grilling 分支）
---

调用 `skill` 工具加载 `shy-skill-suite` 技能，然后按 `references/grilling.md` 对下面的对象做**逼问**：建设计树、一轮问完 frontier、每问附建议答案、等用户回答后再算下一轮。

**对象必填**：`$ARGUMENTS` 未给逼问对象时**直接询问用户**要逼问什么（不默认、不自行挑一个）。

对象：$ARGUMENTS
