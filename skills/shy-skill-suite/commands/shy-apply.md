---
description: 技能开发套件 · 按评审分拣实施「立即修」的项
---

用户已分拣。调用 `skill` 工具加载 `shy-skill-suite` 技能，然后按 `references/lifecycle.md` 阶段 4 执行分拣结果：「立即修」的项实施并回写（勾选验收、追加迭代记录、更新 `status`/`iteration`）；「以后修」的落盘为 `ready` 的新 REQ；「丢弃」的不落盘、不留痕。**实施一次即停，不自动再审。**

**仅在用户完成分拣后执行。**

分拣 / 范围：$ARGUMENTS
