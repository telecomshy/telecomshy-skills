---
description: 技能开发套件 · 把复审意见落盘成 REQ 并开始优化
---

用户已确认落盘。调用 `skill` 工具加载 `shy-skill-suite` 技能，然后按 `references/lifecycle.md` 阶段 4 执行：把 `findings.json` 折回 REQ（勾选验收、追加迭代记录、更新 `status`/`iteration`），或为新发现开 REQ；再进入实现。

**仅在用户显式确认后执行**——没有这一步就停在呈现门禁。

范围 / findings 路径：$ARGUMENTS
