---
description: 技能开发套件 · 只复审一个技能并出报告（不自动改）
---

调用 `skill` 工具加载 `shy-skill-suite` 技能，然后按 `references/reviewing-skills.md` 复审并生成 `findings.json`，再按 `references/running-evals.md` 渲染 HTML 报告。

**只呈现、不落盘、不自动修改**——落盘要等用户显式触发 `/shy-apply`。

技能：$ARGUMENTS
