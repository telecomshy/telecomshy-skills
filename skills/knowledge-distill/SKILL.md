---
name: knowledge-distill
description: 把用户与 AI 的对话内容沉淀为结构化的、可检索、可关联、可长期复用的知识库笔记（默认存为 Obsidian 笔记，也支持普通 Markdown）。在用户明确要求"把对话总结成笔记 / 保存为笔记 / 整理成 note / 沉淀到知识库"时触发；也在用户要求"修改智识沉淀设置 / 变更笔记保存位置或格式 / 重新配置笔记目录"时触发；也在用户要求"查找/检索之前记过的笔记 / 我之前记过什么关于 X 的笔记"时触发。核心是"知识库沉淀"而非"摘要概括"：完整保留对话中的方法、命令、步骤、结论依据、踩坑经验等细节；对话较长且涉及多个相互独立的主题时可拆分为多篇笔记。按分类归档到独立"AI笔记"根目录，用双链关联已有笔记形成知识图谱，每个分类维护一份自动更新的索引文件。
name_cn: 智识沉淀
description_cn: 把对话沉淀为可检索、可关联、可长期复用的知识库笔记（Obsidian/普通Markdown），保留细节、按分类归档、自动维护索引。
create_source: super-agent-skill-creator
---

# 智识沉淀（对话总结为笔记）

## Overview

将一段对话总结为知识库笔记。目标不是"写摘要"，而是**把对话沉淀为可长期使用、可检索复用的知识库条目**：完整保留有价值细节，按分类归档到独立的 `AI笔记` 根目录，用链接关联已有笔记（Obsidian 双链 / 普通 Markdown 标准链接），并自动维护每个分类的索引文件。**当对话较长、涉及多个相互独立的主题时，可拆分为多篇笔记**（偏保守拆分、先给方案一次确认后分别生成），而非固定总结到同一篇。

**触发条件**：
- 用户明确要求"把对话总结成笔记 / 保存为笔记 / 整理成 note / 沉淀到知识库"时触发。
- 用户要求"修改智识沉淀设置 / 变更笔记保存位置或格式 / 重新配置笔记目录"时，走「配置修改」章节。
- 用户要求"查找/检索之前记过的笔记 / 我之前记过什么关于 X 的笔记"时，走「检索已有笔记」章节。
- 用户要求"检查笔记库 / 笔记有没有断链 / 索引和笔记对不对得上 / 整理一下笔记库 / 笔记健康检查"时，走「健康检查」章节。
- 用户要求"给某个主题做一张知识地图 / MOC / 把散落的笔记聚到一起"时，走「生成内容地图（MOC）」章节。

**最高原则（务必遵守）**：
- 不准为了简洁而过度压缩内容。宁可偏详实偏长，也要保留细节（方法、步骤、命令、参数、数值、结论依据、示例、坑与踩坑经验）。
- "排除无关"是指排除与主题无关的寒暄/闲聊，**不是**压缩与主题相关的详细内容。
- **绝不编造事实（红线）**：笔记内容必须忠实于对话中实际给出的信息。AI **不得**自行虚构、补全、美化或改写事实性内容（数据、数字、版本、人名、时间、事件、结论等）。凡是对话没有明确给出、也不是由对话内容可推出的信息，一律不得当作事实写入。确有需要而未获知的信息，标"待核实"或留白并询问用户，**不要**编一个"看起来合理"的说法填上。宁可笔记缺一块，也不要写看似完整却无依据的内容。
- **有外部引用则标注来源**：笔记若引用/依赖了对话之外的外部事实（如某个版本号、某个政策、某组数据、某项外部事件），须注明出处（来源名称或链接），并区分「对话内观点」与「外部核验事实」；无法核实的外部事实标 `(待核实)`。
- 忠实呈现对话给出的内容，不得自行删改或臆造；确有不确定处如实标注。
- 表达风格从内容出发：通俗解释里的类比、举例属于**内容**，不得因"不够严谨"而丢弃；换种说法重复同一句的表达则可归并。取舍的是"说法"，不是"内容"（详见 Step 6「表达风格与讲解方式」）。

---

## 脚本调用约定（重要）

本技能所有脚本命令**必须用绝对路径调用**，不要写成 `python scripts/skill_tools.py`——技能的运行工作目录通常是用户的会话目录，而非技能目录，相对路径会直接失败。

**技能目录解析**：`$TELEAGENT_CONFIG_DIR/skills/knowledge-distill`（`TELEAGENT_CONFIG_DIR` 缺省为 `~/.config/TeleAgent`）。本文档后续用 `<SKILL_DIR>` 代指该目录。

- **推荐写法**（先解析出技能目录，再拼绝对路径）：
  ```
  python "<SKILL_DIR>/scripts/skill_tools.py" <子命令> [参数]
  ```
  例：`python "C:\Users\<用户>\.config\TeleAgent\skills\knowledge-distill\scripts\skill_tools.py" load-config`
- **Windows PowerShell**：调用前先设置 `$env:PYTHONUTF8="1";`，避免中文路径与内容编码错误。
- 路径含空格时必须加引号。
- 若不确定技能目录位置，先用文件搜索定位 `skill_tools.py`，再使用其绝对路径。

---

## 工作流程

### Step 0：读取配置与目录情况

1. 运行 `python "<SKILL_DIR>/scripts/skill_tools.py" load-config` 读取持久化配置。
2. 若配置中有 `vault` 和 `note_root`，先运行 `python "<SKILL_DIR>/scripts/skill_tools.py" list-structure "<vault>\<note_root>"` 查看已有分类与笔记，作为后续判断依据。
   - 返回结果中每个分类含 `notes`（真实笔记）与 `has_index`（是否已有索引文件）两个字段；**`00-分类索引.md` 不会出现在 `notes` 里**，判断"新建 vs 合并"时不要把它当作可合并的笔记。

### 配置存在时的流程（第二次及以后保存）

直接复用配置中的格式与目录，跳过首次配置，从 **Step 3 主题切分** 开始执行。**每次保存都明确告知用户保存到了哪个 vault/目录，并提醒"可通过说明'修改智识沉淀设置'来变更保存位置"。**

### 首次触发（无配置）流程

#### Step 1：确认笔记格式

询问用户的笔记格式，三选一：**Obsidian**（推荐）、**普通 Markdown**、**其它**。

- **Obsidian**：运行 `python "<SKILL_DIR>/scripts/skill_tools.py" discover-vaults` 发现本机 vault。
  - 发现到多个 vault → 列出并询问用户保存到哪个。
  - 发现到单个 vault → 直接采用。
  - 未发现（未装 Obsidian / obsidian.json 不存在）→ 让用户手动填写 vault 绝对路径。
- **普通 Markdown / 其它**：直接询问用户保存目录（绝对路径）。

> 注意：用户机器上可能没有 Obsidian，若无法自动发现且用户也不确定 vault，可推荐用户先明确保存目录，或选普通 Markdown。
>
> **两种格式的差异**（选普通 Markdown 时后续步骤按此执行）：
> | 能力 | Obsidian | 普通 Markdown |
> | --- | --- | --- |
> | 笔记间链接 | `[[笔记名]]` 双链 | `[笔记名](笔记名.md)` 标准链接 |
> | 反向链接 | 原生自动生成 | **无**，需手工补 |
> | frontmatter | 必填 | 不强制，正文开头写标题+日期 |
> | Callout / 高亮 / 块 ID | 支持 | **不支持，不要写** |
> 普通 Markdown 的详细写法见 `references/plain-markdown-best-practices.md`。

#### Step 2：确定并保存配置

1. 确认最终 `format` 与存储位置（vault 或目录）。
2. 笔记根目录名默认 `AI笔记`；若所选目录下已存在 `AI笔记` 目录则直接复用，否则在保存时创建。
3. 将配置写入文件（**推荐用 `--config-file` 传 JSON 文件路径**，避免命令行引号转义问题）：
   ```
   python "<SKILL_DIR>/scripts/skill_tools.py" save-config --config-file <临时json文件>
   ```
   配置结构示例：
   ```json
   {"format": "obsidian", "vault": "D:\\示例路径\\我的笔记库", "note_root": "AI笔记"}
   ```
   普通 Markdown 时用 `"directory"` 字段代替 `"vault"`。

---

### Step 3：主题切分与方案确认（关键决策点）

**本技能支持将长对话按主题拆分为多篇笔记**，而不是固定总结到同一篇。

**核心原则：所有询问集中在本步一次性完成，后续 Step 4~Step 8 直接按确认结果执行，不再逐项追问**（避免一篇笔记来回确认 2-3 轮）。

1. **判断是否需要拆分**：AI 通读整段对话，识别存在哪些**相互独立、各有价值**的主题块。
   - **默认偏保守**：只有主题差异明显、且内容**足够充实、独立成篇才有价值**时才拆分；碎片化、几句话的小话题不单独拆。
   - **同主题合并**：同一主题下的不同维度（如"实现过程"与"踩坑经验"）**合并进同一篇**的不同章节，不拆。
   - **异主题才拆**：仅当主题间差异大、关联弱、各自内容充实时才拆成多篇。
   - 单一主题的对话 → 维持单篇，同样走第 2 步的方案表。
2. **一次性给出完整方案表**：结合 Step 0 的 `list-structure` 结果（已有分类、已有笔记），为**每一篇**拟定标题、分类、新建或合并、一句话概要，合并成一张表让用户**一次性确认或调整**：

   | # | 笔记标题 | 分类 | 新建/合并 | 一句话概要 |
   | - | -------- | ---- | --------- | ---------- |
   | 1 | 示例标题A | 已有分类「技能开发」 | 新建 | 本次对话的技能设计方法 |
   | 2 | 示例标题B | 新分类「示例分类」 | 合并至《示例笔记》 | 本次对话的相关排查经验 |

   - **分类列**：优先归入已有分类并注明；无合适分类时给出新分类建议（避免与已有分类语义重复）；用户可自定。
   - **新建/合并列**：依据已有笔记标题与内容判断能否合并，注明合并目标笔记；判断不了时在该行标注"待确认"。
   - 用户可回复：确认方案 / 全部合并为一篇 / 调整某行的标题、分类或新建合并方式。
3. **确认标题**：方案确认后，各主题的标题即文件名，应语义化、唯一。
4. 后续 **Step 4 ~ Step 8 对方案表中的每一行分别执行**（可各自归入不同/相同分类）；主题间若有相关性，跨笔记用链接互链。
5. 若方案表中存在"待确认"项，或执行中发现新情况（如目标文件重名、合并目标笔记内容不符），再**单独就该项询问用户**，不影响已确认的其它行。

### Step 4：确定分类（按 Step 3 方案表执行）

在笔记根目录下，**一级子目录即分类**。分类在 Step 3 方案表中已确认，本步只做落地：

1. 按方案表为每个主题块确定分类（优先归入已有分类；无合适分类则用确认的新分类）。
   - **避免分类碎片化**：新建分类前先确认确实没有已有分类可容纳。若已有分类语义相近（如"示例分类"与"示例"、"运维"与"运维经验"），优先归入已有分类，而不是再建一个。
   - 若发现已有分类明显重复或命名混乱，可在方案表中**建议**用户合并或重命名，但**不擅自改动**已有目录（改名/合并会牵动已有链接，需用户明确同意后单独处理）。
2. 对**首次创建的分类**：
   - 创建分类子目录即可。**索引文件无需在此手动复制**——Step 7 调用脚本时若发现索引不存在，会自动按 `assets/分类索引模板.md` 渲染创建（含 frontmatter、「笔记导航」「已收录疑问」章节，占位符与示例行一并处理）。
   - 若希望此时就建立索引，也可手动复制模板并替换 `{{分类名}}`；但**两种方式产出一致**，不要重复创建。
   - 无论哪种方式：模板的"笔记导航"表格**默认是空的**（只有表头），**不要**自行补示例行；具体条目由 Step 7 统一写入。模板中的示例说明文字（如"（此处分点记录…）"）应替换为真实内容或删除，不得残留。

### Step 5：执行"新建"或"合并"（按 Step 3 方案表执行）

新建/合并方式在 Step 3 方案表中已确认，本步按行执行：

1. **写前查重与文件名校验（必须，且必须在写笔记与更新索引之前）**：对每篇笔记先运行
   `python "<SKILL_DIR>/scripts/skill_tools.py" check-name "<分类目录>" "<标题>"`
   - `valid: false` → 标题含 `/` `:` `*` `?` 等非法文件名字符（含 `/` 时会被当成子目录、路径失真），**先按返回的 `suggested_title` 改名**再重试，或与用户确认新标题。
   - `exists: false` → 标题可用，记下最终标题，继续后续步骤。
   - `exists: true` → **不得直接覆盖**，向用户确认三选一：**覆盖** / **改名**（另取标题）/ **合并**进该笔记（走本步第 5 点合并规范）。
   - 注意 Obsidian 中同名笔记会让 `[[笔记名]]` 双链产生歧义，重名时建议优先改名，或改用 `[[分类/笔记名]]` 形式链接。
   - **本步确认的最终标题就是后续写入文件名与索引条目的标题**，三者必须一致；先查重再更新索引，可避免"索引已写入、笔记却改名"的不一致。
2. 在确定分类下（必要时跨分类）核对已有笔记（`list-structure` 已返回各分类笔记）。
3. **用内容检索判断能否合并**：仅看标题容易漏判，对本次主题的核心关键词运行
   `python "<SKILL_DIR>/scripts/skill_tools.py" search-notes "<note_root>" "<关键词>" [--category "<分类>"]`
   - 命中已有笔记且主题一致 → 方案表该行标为"合并至《目标笔记》"。
   - 无命中或主题差异大 → 新建。
   - 关键词不确定时可用 `--regex` 传同义词正则（如 `"关键词A|关键词B"`）。
4. **新建**：走 Step 6 撰写并写入流程。
5. **合并**（严格按以下规范，避免破坏已有内容）：
   - **只追加、不重写**：在目标笔记中**新增章节或插入段落**，不重写、不删除既有段落；既有内容即使与本次重复也保留（可在新章节说明"补充"）。
   - **插入位置**：优先追加到笔记末尾的"相关笔记"章节之前；若新内容明显属于某个已有章节，则插入该章节末尾。
   - **元数据**：在 frontmatter 追加或更新 `updated: YYYY-MM-DD`；保留原有 `tags`，可补充新标签。
   - **来源标注**：合并的新内容可加一行来源说明（如"来源：2026-09-09 对话"），便于追溯。
   - **不覆盖用户手改**：若发现目标笔记内容与预期差异较大（可能被用户改过），先向用户确认再合并。
   - **写入方式**：先读取目标笔记原文、在内存中拼好合并后的完整内容，再用 `write-note` **整体写回**（见 Step 6）——脚本会先备份原文件、再原子替换，避免写到一半损坏原笔记；**不要**用其它方式直接改写已有笔记。
6. 用户追问的相似问题 → 优先归入已收录该主题的那篇笔记，**不新建**。

### Step 6：撰写并写入笔记（核心，每个主题块分别执行）

按"知识库沉淀"原则撰写，禁止过度概括。对**每个拆分后的主题块**分别撰写一篇。要点：

- **保留细节优先于结论**：方法、步骤、命令/代码、参数、配置、数值、结论依据、示例、注意事项、坑等要写全。不能只留"解决了X问题"这种结论句。
- **排除无关**：只剔除与主题无关的寒暄/闲聊，主题相关内容完整保留（详细≠无关）。
- **聚焦用户疑问**：用户提问是疑惑集中点，完整呈现其解答与依据、延伸信息。
- **表达风格与讲解方式**（在"照搬口语"与"过度压缩"之间取中）：笔记文风随**内容与用户指令**定，不随对话的随口程度漂移。
  - **主基调篇内一致，明确指令优先**：一篇笔记只用一种叙述基调，不因某一轮对话的表述方式整篇改文风。用户明确说"这篇用白话写 / 写得严谨些"就照办；优先级为「当次明确指令 > 本次会话已确立的风格 > 笔记默认体例」；用户要求整篇用某风格时，就按该风格写主表述，不必再另堆一种写法。
  - **通俗解释按"是否新增理解路径"取舍**：对话中的类比、大白话、举例，若把抽象概念映射到具体经验、帮助建立直觉（例：用"按电梯按钮一次与多次结果相同"解释幂等）→ 保留；若只是换种说法重复同一句、删掉不损失理解 → 可略。尺度是"有无新增理解路径"，**不是**"越全越好"，也**不是**因"不够严谨"而删；通俗化只做加法或等价替换，不得把原意讲走样。
  - **补充性通俗解释独立成块，不覆盖主表述**：用户只是要求"某个概念用白话再解释一下"时，把通俗版作为独立的提示块/小节保留，**不要**覆盖或替换原有的准确表述，也不要把整篇改写成白话——同一内容重复两遍才是冗余。提示块有明确标记，不视为"换腔调"，与"篇内基调一致"不冲突。
  - **呈现形式随格式**：Obsidian 用 callout（如 `> [!tip] 通俗理解`）；普通 Markdown 用 `> **通俗理解**：……`（普通 Markdown 不支持 callout）。
  - **风格要求本身不进笔记**："用户要求这样讲"属于对话过程，不构成知识，不写入正文（同 `references/obsidian-best-practices.md` 中"不谈 AI/LLM 话题本身"的原则）。
- **结构化**：标题层级、分点、代码块、表格组织清晰，便于检索。
- **Obsidian 笔记**：遵循 `references/obsidian-best-practices.md`（frontmatter、双链、callouts、标签等）。不确定语法时参考 `assets/obsidian-markdown-docs/` 下官方文档。
  - **frontmatter 必填字段**：`tags`（YAML 列表，至少含分类名与主题关键词）、`created`（YYYY-MM-DD）、`source`（如"对话总结"）。
  - **可选字段**：`aliases`（中文标题建议补充别名，便于检索）、`updated`（仅合并更新时写入）。
- **普通 Markdown 笔记**：遵循 `references/plain-markdown-best-practices.md`。要点：
  - **无 frontmatter 要求**，但正文开头写标题与日期（如 `> 创建：YYYY-MM-DD　来源：对话总结`）。
  - **只写标准 Markdown（CommonMark）子集**：**不要**写 Obsidian 专属语法——`[[双链]]`、`==高亮==`、`> [!callout]`、`^块id`、`![[嵌入]]` 在普通 Markdown 阅读器里都会失效或显示为原始文本。
  - 需要强调注意时（注意、通俗理解、信息不完整等凡需提示块的场景）统一用加粗前缀引用块 `> **前缀**：……`（如 `> **注意**：……`）替代 callout。
  - 笔记间链接用 `[标题](标题.md)`；标题含空格时用 `[标题](<含 空格.md>)` 或 `%20` 编码。
- **关联**：文中提到的既有概念/笔记用链接关联，无法链到概念时保持内容自洽，后续可回补。**本次拆分生成的多篇笔记若主题相关，也须互相链接**（在"相关笔记"章节列出彼此）。
  - **主动回链旧笔记（沉淀即检索）**：把 Step 5 用 `search-notes` 检索到的**同主题旧笔记**，在「相关笔记」章节回链，新笔记一落库就与既有知识连上；旧笔记这侧不改（Obsidian 有原生反向链接）。
  - **Obsidian 格式**：只在本篇写**单向**双链 `[[笔记名]]`，**不要**去修改其它已有笔记补反向链接——Obsidian 原生会自动生成反向链接（Backlinks 面板与 Graph view），手工补是冗余，且会写到用户自己创建的笔记里，与"AI 笔记独立存放"的原则冲突。
  - **普通 Markdown 格式**：没有自动反链能力，需**手工补反向链接**——在被引用的笔记（仅限 `AI笔记` 目录内的笔记）末尾「相关笔记」章节补一行 `[标题](标题.md)`；**不要**改动用户自己创建的笔记。只补本次实际引用到的关联，不滥建。
- **与旧笔记冲突时先标注、不静默覆盖（必须）**：撰写前用
  `python "<SKILL_DIR>/scripts/skill_tools.py" search-notes "<note_root>" "<主题关键词>" [--full]`
  拉出同主题旧笔记（加 `--full` 可看到完整正文），**由你判断**新内容与旧笔记是否**结论冲突**（同一问题说法不一致）。
  - 发现冲突 → **不要**悄悄改写旧结论、也不要只按新说法写：在**本次笔记**里用提示块显式标注冲突（Obsidian `> [!warning] 与 [[旧笔记名]] 存在冲突`；普通 Markdown `> **冲突提示**：与 [旧笔记名](旧笔记名.md) 存在冲突`），并简述两种说法各自的依据；同时在索引「已收录疑问」登记该疑问（Step 7），便于日后一次性回看裁定。
  - 若必须**改写旧笔记**才能消解冲突，**先向用户确认**，不擅自改动。
  - 说明：冲突判断属语义工作，脚本只负责把相关笔记取出来，判断与标注由 LLM 完成、结果显式可见——宁可把矛盾摆在明面上，也不要让知识库静默自相矛盾。
- **真实性**：忠实呈现，不臆造；不确定处如实标注。
  - **区分事实与推断（必须）**：写笔记时严格区分「对话里明确说的」与「AI 自己推的」，避免无依据内容混入知识库后被当成事实：
    - 对话中明确给出的 → 直接写，不加标记。
    - 由上下文推断得出 → 加 `(推断)` 标记，并在同一处简述推断依据。
    - 无法核实的外部事实 → 加 `(待核实)` 标记。
    - 依据：知识库最怕的不是缺失，而是看似完整但无依据的内容——后者会污染后续判断。
  - **易变事实标注日期（必须）**：涉及数量、版本、上线状态、进度这类会随时间变化的信息，写成 `……（as of YYYY-MM-DD）`，避免日后被误读为当前现状。与「区分事实与推断」互补：一个解决「可信吗」，一个解决「还成立吗」。
  - **上下文不完整时降级**：若对话已被截断、压缩或明显缺少关键信息（如命令只出现一半、结论没有依据），**不要凭常识补全或猜测**。做法：基于现有信息如实撰写，在缺失处用提示块标注——Obsidian 用 `> [!warning] 信息不完整`，普通 Markdown 用 `> **信息不完整**：……`——并说明缺什么、建议用户补充；必要时先向用户确认。
  - 宁可留白并标注，也不要写看似完整但无依据的内容——后者会污染知识库，比缺失更有害。
  - **外部事实首选核验，且只核验不沉淀（必须）**：笔记中出现**客观的、可核实的、易失效或易错**的外部事实（如版本号、系统/产品名称、数据、指标、政策条款、外部事件时间等）时，若对话本身未给出充分依据、**且你对其准确性不确定**，**必要时调用搜索工具联网核验**，并把核验到的出处（来源名称或链接）标注在笔记中。
    - **仅高价值/易失效事实才搜**：只对"错了会影响使用、或已过时"的关键外部事实核验；方法论、经验、命令、排查思路等对话本身已讲清的内容，**不要**为核验而搜索，避免拖慢沉淀。
    - **只验证不沉淀**：搜索只用于**核验准确性、标注出处**，**不要**把搜到的网页/文档内容整篇或大段摘录沉淀进笔记，也不把它当作新的"知识来源"扩展笔记内容——本技能沉淀的对象仍是对话本身（与"设计边界-外部资料接入不做"一致）。
    - **标注方式**：核验后若与对话一致，可在该事实旁注明来源（如 `（来源：○○官网）` 或给出链接）；若搜索发现对话内容有误，按"与旧笔记冲突"一节的思路处理——在笔记中如实标注更正与依据，必要时询问用户；若搜索仍未能核实，标 `(待核实)`。
- **超长笔记先分段落草稿、再整体写入**：单篇内容很长时，先在临时草稿里分段补齐（避免一次性生成过长内容被截断），最后**整体**走 `write-note` 写入正式笔记——不要对着正式笔记分多次覆盖写。
- **写入文件（统一走 `write-note`，事务化写入）**：按 Step 5 确认的最终标题，先把完整内容写入一份临时草稿文件，再运行
  ```
  python "<SKILL_DIR>/scripts/skill_tools.py" write-note "<note_root>/<分类>/<标题>.md" --content-file "<草稿文件>"
  ```
  - 脚本会：目标已存在则**先备份**到 `~/.knowledge-distill-backups/`；先写同目录临时文件、再用原子替换（写到一半失败不会损坏原笔记）；内容与原文完全一致时返回 `unchanged`，不写、不备份。
  - 返回 `action` 为 `created` / `overwritten` / `unchanged`，`backup` 为备份文件路径（无备份时为 `null`）。
  - **所有整篇写入/重写都必须走本命令**（含 Step 5 的"合并"：读原文 → 内存拼好 → 整体写回），不要用别的方式直接覆盖 `md` 文件。Obsidian 用双链、普通 Markdown 用标准链接/相对路径组织。

### Step 7：更新分类索引文件

`00-分类索引.md` 以**笔记为单位**做导航，采用"去重聚合"策略。对**本次生成的每篇笔记**分别更新其所在分类的索引。

**用脚本写入，不要手写表格**（保证列格式与链接写法稳定）：
```
python "<SKILL_DIR>/scripts/skill_tools.py" append-index-entry "<分类目录>" "<笔记标题>" "<一句话摘要>"
```
脚本会自动处理：文件不存在则按模板新建（结构与「Step 4」模板路径一致，含 frontmatter 与「已收录疑问」章节）、已存在同名条目则更新摘要、否则追加一行；返回值 `action` 为 `created` / `appended` / `updated` / `unchanged`。标题或摘要中的竖线、换行会被自动转义为表格安全形式，**不要自行再转义**。

- **链接写法随格式自动切换**（脚本读配置决定，一般无需干预）：Obsidian 生成 `[[标题]]` 双链；普通 Markdown 生成 `[标题](标题.md)` 标准链接。若配置缺失或需强制指定，可加 `--format obsidian|markdown`。
- 因此**不要手工把索引里的链接改成另一种写法**——下次脚本更新该行时会被覆盖回配置格式。

规则：
- **全新主题疑问** → 脚本追加一行（标题 + 一句话摘要 + 指向笔记的链接）。
- **相似/追问问题**（已收录在某笔记）→ **不新增条目**，只在对应笔记深化内容。
- 合并场景同样同步更新索引条目与摘要。
- 需要先查看当前索引时：`python "<SKILL_DIR>/scripts/skill_tools.py" list-index "<分类目录>"`。
- 若拆分后的多篇笔记分属多个分类，则分别对其所在分类调用脚本。

**「已收录疑问」章节**：该章节记录本分类覆盖到的关键疑问，每条**只放疑问 + 指向笔记的链接**（与模板注释一致），便于点击跳转。**不要把回答写进索引**——回答正文留在笔记里，索引再抄一份会与笔记不同步。同样用脚本写入，不要手写。

**写之前必须先查重**（关键，避免"同义不同文"变成多条冗余）：

```
python "<SKILL_DIR>/scripts/skill_tools.py" list-questions "<分类目录>"
```

读取本分类已收录的疑问（返回每条疑问文本与已并列的笔记链接），逐条比对：

- 已有**语义等价**的疑问 → **复用该条的原疑问文本**再调用写入命令，脚本会命中该条并合并链接（不要换个说法另起一条）；
- 没有等价的 → 作为新疑问写入。

写入：

```
python "<SKILL_DIR>/scripts/skill_tools.py" append-index-question "<分类目录>" "<疑问>" "<笔记标题>"
```

脚本统一生成 `- 疑问 → <链接>[、<链接>...]` 格式（Obsidian 为 `[[笔记标题]]`，普通 Markdown 为 `[笔记标题](笔记标题.md)`，读配置自动决定）；返回值 `action` 为 `appended` / `updated` / `unchanged` / `section_created`，`links` 为该疑问当前并列的全部笔记。

- **一条疑问可指向多篇笔记**：脚本对同一疑问**合并链接**（保留已有、追加新的、去重、保持顺序），既不覆盖已有指向，也不重复成多条。笔记按主题各自成篇，同一疑问若确实横跨多个主题，就把相关笔记**都列出来**，读者按需跳转；**不要**为了"只留一个链接"而把不同主题的笔记强行合并。
- 若一条疑问已铺开到 4 篇以上（脚本会返回 `warning`），通常说明该疑问过于宽泛：宜拆成更聚焦的疑问，或确认这些笔记是否主题重复、可以合并。
- 只记录**用户真正提出过、且已在本分类笔记中解答**的疑问，不要凭空编造。同一疑问合并为一条。
- 疑问与笔记导航条目是两套视角：导航按"笔记"去重，疑问按"用户提问"去重，两者都要维护。
- 若某条旧疑问还是"纯文本回答、无链接"的旧格式，脚本会在下次写入命中它时自动规范为链接格式（回答本就应保留在笔记里，索引不重复承载）。

### Step 8：结果确认

1. **告知用户**保存结果：共生成几篇笔记，逐篇列出格式、vault/目录、分类、标题、完整文件路径；并提醒"可修改智识沉淀设置以变更保存位置"。
2. 若过程中任何判断不确定，**主动询问用户**，不擅自决定。
3. 若写入过程中出现失败（如权限、路径异常），如实告知并保留已完成的成果，不要静默跳过。
4. **顺带回报相关旧笔记（沉淀即检索）**：保存完成后，用本次主题关键词运行
   `python "<SKILL_DIR>/scripts/skill_tools.py" search-notes "<note_root>" "<主题关键词>"`
   把命中的**其它旧笔记**（分类 + 标题 + 路径 + 片段）一并列出，提示"这些旧笔记可能与本次相关"。
   - 这是把"沉淀"与"检索"接起来，用户不必再单独问一句"我之前记过什么"；只读检索，不生成任何新笔记。
   - 若本次笔记**已回链某篇旧笔记**，在该篇后注明"已互链"；若发现主题明显重合，可建议下次合并。

---

## 检索已有笔记

当用户问"我之前记过什么关于 X 的笔记""帮我找一下之前的笔记"时，不生成新笔记，只做检索与回报：

1. 读取配置拿到 `vault`/`directory` 与 `note_root`（`load-config`）；无配置则说明尚未保存过笔记。
2. 运行
   `python "<SKILL_DIR>/scripts/skill_tools.py" search-notes "<note_root>" "<关键词>" [--category "<分类>"] [--regex]`
   - 关键词不确定时用 `--regex` 传同义词正则；需要更精确时用 `--max-snippets`、`--context` 调整片段数量与长度；需要看到某篇笔记的**完整正文**时加 `--full`（用于读取旧笔记后带引用作答）。
   - 也可先 `list-structure` 看有哪些分类与笔记，再决定检索范围。
3. 向用户回报：命中的笔记（分类、标题、完整路径）与相关片段；**只读不写**，不修改任何笔记。
4. 若无命中，如实告知，并询问是否需要就当前内容新建笔记（此时才走 Step 3 起的保存流程）。

---

## 生成内容地图（MOC）

分类目录回答"笔记放在哪个抽屉"，MOC（Map of Content，内容地图）回答"关于这个主题我知道些什么"。当用户要求"给 X 做个知识地图 / MOC / 把散落的笔记聚到一起"时，按主题生成一页**跨分类**的链接清单：

```
python "<SKILL_DIR>/scripts/skill_tools.py" gen-moc "<note_root>" "<主题>" [--tag "<标签>"] [--keyword "<关键词>"] [--category "<分类>"] [--description "<说明>"]
```

1. **匹配规则**：优先用 `--tag` 按 frontmatter 标签精确匹配；否则用 `--keyword`（缺省为 `<主题>`）在标题、frontmatter、正文中做关键词命中；`--category` 可把范围限定在单个分类。
2. **输出位置**：写到笔记根目录下的 `MOC-<主题>.md`（与分类子目录同级）。因此 `list-structure` / `lint-notes` / `search-notes` 只处理分类子目录，**MOC 不会被误当作笔记，也不会报"未收录/孤立"**。
3. **可反复刷新**：MOC 由脚本生成，重复运行即重新聚合（已存在会先备份、再原子替换）；页面内也注明了"请勿手工编辑（下次生成会覆盖）"。
4. **按需生成，不自动维护**：只有用户要求时才生成；不要为每篇笔记都自动建 MOC。
5. **回报**：告知用户 MOC 文件路径、聚合到的篇数与各分类分布；若某篇相关笔记没被聚合进去，提示可换标签/关键词重新生成（笔记标题或标签变动后，重新运行 `gen-moc` 即可刷新）。

---

## 健康检查

当用户要求"检查笔记库 / 有没有断链 / 索引和笔记对不对得上 / 整理一下笔记库"时，运行只读检查并回报，**不自动修改任何文件**：

1. 读取配置拿到 `vault`/`directory` 与 `note_root`（`load-config`）；无配置则说明尚未保存过笔记。
2. 运行
   `python "<SKILL_DIR>/scripts/skill_tools.py" lint-notes "<note_root>"`
3. 脚本只报告问题、不改文件，返回八类结果，分「链接结构」与「内容质量」两组：

   **链接结构（五类）**：
   - `index_orphans`：索引「笔记导航」里收录、但文件已不存在（用户手删笔记后索引未同步）。
   - `broken_links`：笔记里的链接指向不存在的笔记（笔记改名后别处未更新）。
   - `unindexed_notes`：笔记存在、但所在分类索引的「笔记导航」里没有收录。
   - `orphan_notes`：既没有入链、也未被索引收录的孤立笔记（等于"写了但找不到"）。
   - `question_orphans`：索引「已收录疑问」里并列的链接指向不存在的笔记。
   - **链接兼容**：同时识别 `[[双链]]` 与 `[文字](笔记.md)` 两种写法，图片、外链、纯锚点会自动跳过，不会误报。因此 Obsidian 与普通 Markdown 两种笔记库都可用同一命令检查。

   **内容质量（三类）**：
   - `missing_frontmatter`：Obsidian 笔记缺 frontmatter，或 frontmatter 缺必填字段（`tags` / `created` / `source`）。**仅当用户已配置为 Obsidian 格式时检查**——普通 Markdown 无 frontmatter 要求，不会误报。
   - `empty_sections`：有标题、标题下却没有任何正文的章节（骨架写了没填）。
   - `stale_index`：笔记比其所在分类的索引文件更新（改了笔记、索引摘要却没同步）。
   - 返回的 `summary` 除 `issues`（问题总数）外，另含 `structure_issues`、`content_issues` 两个分组计数，便于一眼看出问题规模。
4. 向用户逐类列出问题清单（含完整路径），并**询问是否要修复**：
   - 索引类问题（`index_orphans` / `unindexed_notes` / `question_orphans`）可用 `append-index-entry`、`append-index-question` 等命令修正，但**必须先经用户确认**。
   - 断链问题需要用户决定是"改链接"还是"补建目标笔记"，不要擅自改。
   - 内容质量类：`empty_sections` 需补齐正文、`missing_frontmatter` 需补字段，都走 `write-note` 重写该篇；`stale_index` 用 `append-index-entry` 刷新摘要。同样**一律先经用户确认再动手**。
5. 若八类问题均为空，明确告知"笔记库健康，未发现问题"。

> 设计取舍：只做**确定性检查**（文件存在性、链接目标、索引收录、frontmatter 字段、空章节、索引时效），不做语义判断，因此结果准确、可复现，适合脚本化。修改动作一律交回用户确认，避免误改用户内容。

---

## 配置修改

用户要求修改保存位置/格式时，更新配置文件（Step 2 的 save-config），并告知新的保存位置。

## 脚本说明

`<SKILL_DIR>/scripts/skill_tools.py` 提供确定性操作，建议 LLM 通过命令行调用：
- `discover-vaults`：发现本机 vault
- `load-config` / `save-config`：读写配置（save-config 推荐 `--config-file`）
- `list-structure <note_root>`：列出分类与笔记（索引文件不计入 `notes`，用 `has_index` 标记）
- `ensure-root <base> --name AI笔记`：检查/创建笔记根目录（`created` 表示本次是否真正新建）
- `list-index <分类目录>`：读取分类索引
- `list-questions <分类目录>`：列出「已收录疑问」条目（含各条已并列的笔记链接）；写入新疑问前用它查重，避免"同义不同文"重复
- `check-name <分类目录> <标题>`：写入前检查同名笔记并校验标题能否安全用作文件名，返回 `valid`（标题是否合法）、`exists`，避免误覆盖与非法路径
- `search-notes <note_root> <query>`：在笔记正文中检索（`--category` 限定分类、`--regex` 用正则），返回命中片段；加 `--full` 额外返回命中笔记的完整正文
- `write-note <note_path> --content-file <草稿>`：事务化写入笔记——已存在则先备份、临时文件原子替换、内容相同返回 `unchanged`；返回 `action` / `backup` / `bytes`
- `gen-moc <note_root> <topic>`：按 `--tag` 或关键词跨分类聚合，生成/刷新笔记根目录下的 `MOC-<主题>.md`（`--tag` / `--keyword` / `--category` / `--description`）；重复运行即刷新，已存在会先备份
- `lint-notes <note_root>`：只读健康检查，返回链接结构（索引孤儿 / 断链 / 未收录 / 孤立 / 疑问断链）与内容质量（缺 frontmatter / 空章节 / 索引过期）共八类问题，不修改文件
- `append-index-entry <分类目录> <标题> <摘要>`：向分类索引追加/更新一行笔记导航，保证表格格式稳定；链接写法读配置自动决定（`--format` 可强制）
- `append-index-question <分类目录> <疑问> <笔记标题>`：向分类索引的「已收录疑问」追加/更新一条，统一生成 `- 疑问 → <链接>[、<链接>...]` 格式（Obsidian 双链 / 普通 Markdown 标准链接）；同一疑问会**合并链接**（保留已有、追加新的、去重），不覆盖、不重复，因此一条疑问可并列指向多篇笔记

---

## 设计边界（明确不做的事）

本节记录**刻意不做**的功能及理由，作为后续迭代的护栏，避免重复讨论或范围蔓延。新增功能前先读本节。

**暂缓（达到触发条件再做）**：

| 事项 | 暂缓理由 | 触发条件 |
| ---- | -------- | -------- |
| 操作日志 `log.md`（每次操作追加记录） | 增加维护动作，当前笔记量小、追溯需求弱 | 笔记超过约 30 篇，或出现"想不起什么时候记的"时 |
| 修正全库清扫（同一事实被修正后批量改写其它笔记） | 需语义搜索 + 多文件批量改写，工程量大，个人库规模下收益有限 | 同一事实被 3 篇以上笔记复述、且确需统一修正时 |
| `search-notes` 结果排序 | 当前按文件顺序返回，个位数笔记量下完全够用 | 笔记超过约 50 篇，或出现"搜出太多结果挑不过来"时 |

**明确不做**：

| 事项 | 不做的理由 |
| ---- | ---------- |
| 向量 / 语义检索 | 生产验证结论：在 5-10 万 token 的笔记规模下，关键词 + 读取比向量检索更可靠、更可解释，且无额外依赖 |
| 跨工具兼容（适配多种 Agent 平台） | 本技能定位为 TeleAgent 单平台，跨平台适配会引入大量兼容分支，收益与成本不匹配 |
| 来源哈希追踪 | 需要维护哈希表、内容一变即失效，个人笔记场景下追溯价值低 |
| 置信度数值评分 | 数值化置信度易被误读为精确度量；用 `(推断)` `(待核实)` 定性标注已足够 |
| 疑问的语义判重自动化 | 判断两条疑问是否"同一个意思"需要语义理解，脚本做不准也难解释；脚本只提供 `list-questions` 列出已有条目，由 LLM 比对后决定复用还是新增 |
| 笔记文风偏好写入长期记忆 | 笔记文风应随内容与当次指令定；把"都写白话 / 都写严谨"固化成长期偏好，会与"笔记需与内容匹配"冲突。用户在**对话交互**层面的偏好由记忆系统负责，不由本技能固化笔记文风 |
| 自动 hooks / 定时任务（含"笔记库定期巡检"） | 属于平台（harness）层能力，不应写进技能。TeleAgent 已有定时任务机制，技能保持"按需触发"即可；定期巡检若需要，由用户在对话里说一句"检查笔记库"手动触发（或自行在平台层配定时任务） |
| 来源与置信度的结构化字段（`source_date` / `source_type` / `source_ref` / `confidence`） | 与现有字段重复：`source_date`≈`created`、`source_type`≈`source`；`confidence` 数值化置信度与既有的 `(推断)` / `(待核实)` 定性标注重复；`source_ref` 仅在"外部资料接入"场景才有意义（该场景已不做）。再引入只会稀释写作注意力、增加歧义 |
| 外部资料 / 网页接入沉淀 | 本技能定位是"沉淀对话"；接入网页需额外处理抓取、去重、时效与版权，且市面已有成熟的抓取类技能，超出本技能定位 |

> **关于"搜索核验"与"不沉淀外部资料"的边界**：技能允许对**客观、易失效的外部事实**（版本号、数据、政策等）联网核验准确性并在笔记中标注出处（见 Step 6「外部事实首选核验」），但这**只是核验与出处标注，不是外部资料接入沉淀**——搜索到的网页/文档内容不会被整篇或大段摘录进笔记，笔记的知识来源仍是对话本身。二者不冲突。